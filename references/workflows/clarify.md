# Clarify

Read the repository, host instructions, workspace/project.md, existing plan and recorded clarifications. Use the graph when available and external sources only for missing external facts.

Distinguish discoverable facts from decisions only the user can make. Ask the remaining material questions together; do not repeat answered questions. State justified defaults for optional decisions.

When writes are allowed, record the questions and answers with parameterized writes linked to the selected plan (or null before a plan exists). Continue only the workflow the user requested.
